# Configure estas variáveis no seu arquivo .env.local (não comitado)

# Supabase
VITE_SUPABASE_URL=https://SEU_PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=sua_anon_key_aqui

# Anthropic (usada apenas na Edge Function, NÃO exposta ao frontend)
# Configure como secret no Supabase: supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
# Aqui só para referência local; nunca comitar a chave real.
